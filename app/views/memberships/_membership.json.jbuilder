json.extract! membership, :id, :client_id, :fechainicio, :fechaexpiracion, :service_id, :rate_id, :entradasrestantes, :estado, :created_at, :updated_at
json.url membership_url(membership, format: :json)
