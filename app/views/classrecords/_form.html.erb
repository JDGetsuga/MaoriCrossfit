<%= form_for(@classrecord) do |f| %>
  <% if @classrecord.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@classrecord.errors.count, "error") %> prohibited this classrecord from being saved:</h2>

      <ul>
      <% @classrecord.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :membership_id %><br>
    <%= f.text_field :membership_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
